import { LoginArea } from '../templates/LoginArea';

export const Login = () => {
  return (
    <div className="flex justify-center items-center w-screen h-screen top-bg">
      <LoginArea />
    </div>
  );
};
